package test;
import test.structures.*;

component BumpBot{

	ports	out Ego_Vehicle test,
		//in Vehicle_Status vehicle_status,
		in Collision collision;

	implementation Math{
		static Vector3 collision_normale = collision.normal_impulse;
		//static Quaternion orientation;
		//collision_normale = collision.normal_impulse;
		//orientation = vehicle_status.orientation;

		static B collisionHappened = false;
		test.throttle = 1.0;
		test.brake = 0.0;
		test.hand_brake = false;
		test.reverse = false;
		test.gear = 1;
		test.manual_gear_shift = false;		
		//if normal_impulse > 0.0 //if a collision occurred drive into the other direction
			test.reverse = true;
		//end
	}
}
