package mnistvae;

component Decoder{
    ports in Z(0:255)^{2} sample,
          out Q(-oo:oo)^{784} res;

    implementation CNN {
        sample ->
        FullyConnected(units=400) ->
        FullyConnected(units=784) ->
        res;
    }
}