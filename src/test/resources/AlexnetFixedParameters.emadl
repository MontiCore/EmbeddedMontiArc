component AlexnetFixedParameters {
    ports in Z(0:255)^{128, 128, 3} image,
         out Q(0:1) predictions[10];

    implementation DeepLearning {

        context = gpu
        architecture = image --> Alexnet --> predictions
        training = SimpleConfig

    }
}