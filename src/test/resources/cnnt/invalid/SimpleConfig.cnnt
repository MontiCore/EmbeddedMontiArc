/* (c) https://github.com/MontiCore/monticore */
configuration SimpleConfigInvalid{
    num_epochs:1
    batch_sizes:64
    normalizes:true
    load_checkpoint:false
    optimizer:adam{
        learning_rate:0.01
        learning_rate_decay:0.8
        step_size:1000
        weight_decay: 0.01
    }
}
