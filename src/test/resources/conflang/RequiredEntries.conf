/* (c) https://github.com/MontiCore/monticore */
package de.monticore.lang.conflang;

configuration RequiredEntries {

    also_required: 0.1
    also_required_enum: some_constant
    also_required_nested: some_value {
    }
}