/* (c) https://github.com/MontiCore/monticore */
package de.monticore.lang.schemalang;

schema RepeatedRequiresRules {
    noise_input requires num_epoch
    noise_input requires num_epoch
    normalize requires batch_size
    normalize requires batch_size
}