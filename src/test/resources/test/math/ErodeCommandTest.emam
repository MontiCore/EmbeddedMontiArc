/* (c) https://github.com/MontiCore/monticore */
package test.math;

component ErodeCommandTest{
    port
    in Q^{n,m} arrayIn,
    out Q^{n,m} arrayOut;

    implementation Math{
        arrayOut = erode(arrayIn);
    }
}
