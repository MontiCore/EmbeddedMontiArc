/* (c) https://github.com/MontiCore/monticore */
package test.emam.optimization;

component MinimizePortsTest{

    ports in Q u,
          out Q y;

    implementation Math{
        Q y1 = minimize(u)
            (u*u) - 2 * u + 1;
        subject to
            u >= 0;
        end
    }
}
