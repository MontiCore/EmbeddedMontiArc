/* (c) https://github.com/MontiCore/monticore */
package lib;

stream STQ_TestWrapperStream for STQ_TestWrapper {
    in1: 0.1 tick 0.5 tick 1 tick 0.9 tick 0.75 tick 0.6;

    out1: false tick false tick true tick true tick true tick false;
}
