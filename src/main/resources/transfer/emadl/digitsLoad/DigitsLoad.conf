/* (c) https://github.com/MontiCore/monticore */
configuration DigitsLoad{
     num_epoch:2
     batch_size:64
     normalize:false
     context:cpu
     load_checkpoint:false
     optimizer:sgd{
         learning_rate:0.05
         learning_rate_decay:0.85
         step_size:1000
         weight_decay:0.0
     }
}
