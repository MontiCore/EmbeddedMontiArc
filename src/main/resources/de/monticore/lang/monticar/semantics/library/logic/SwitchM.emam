/* (c) https://github.com/MontiCore/monticore */
package de.monticore.lang.monticar.semantics.library.logic;

component SwitchM<T> {
	port 
		in T in1, //if
	    in T cond, // condition
		in T in3, //else
		out T out1;
}
