package de.rwth.ba;

component CVGaussianBlur<N n,N m> {
    port
        in Q^{3,n,m} src,
        in Z sizeX,
        in Z sizeY,
        in Q sigmaX,
        in Q sigmaY,
        out Q^{3,n,m} dst;

    implementation Math {
        dst = gaussianBlur(src, sizeX, sizeY, sigmaX, sigmaY);
    }
}