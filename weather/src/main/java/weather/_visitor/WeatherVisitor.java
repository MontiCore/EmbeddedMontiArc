/* generated by template visitor.SimpleVisitor*/



package weather._visitor;

import java.util.Iterator;
import weather._ast.ASTWeatherNode;
import de.monticore.ast.ASTNode;

/**
 * Default AST-visitor for the {@code Weather} language.<br/>
 * <br/>
 * <b>Running a visitor</b>: Starting a traversal of an AST with root {@code astNode} is as simple as calling {@code handle(astNode)}. Note that the visitor only handles nodes of language {@code Weather}.<br/>
 * <br/>
 * <b>Implementing a visitor:</b><br/>
 * You should never use {@code this}, but always make use of {@link #getRealThis()}. This ensures that the visitor can be reused by composition.<br/>
 * <br/>
 * <ul>
 *   <li><b>Visiting nodes</b>: You may override the {@code visit(node)} and {@code endVisit(node)} methods to do something at specific AST-nodes.<br/><br/></li>
 *   <li><b>Traversal</b>: You may override the {@code traverse(node)} methods, if you want to change the climb down strategy for traversing children (e.g. ordering the children). Be aware of the underlying double-dispatch mechanism: probably you want to call {@code childNode.accept(getRealThis())} and <b>not</b> {@code handle(childNode)}<br/><br/></li>
 *   <li><b>Handling of nodes</b>: You may override the {@code handle(node)} methods, if you want to change its default implementation (depth-first iteration): {@code visit(node); traverse(node); endVisit(node);}<br/><br/></li>
 * </ul>
 * <b>Special node type {@code ASTNode}:</b><br/>
 * Visitors do not provide handle or traverse methods for {@code ASTNode},
 * because handling and traversal are defined in the language depending node
 * types. However, an {@link WeatherInheritanceVisitor} visits and
 * endVisits each node as {@code ASTNode}. Due to composition of all kinds of
 * visitors we must define the methods here in the main visitor interface.
 *
 * @see ASTWeatherNode#accept(WeatherVisitor visitor)
 */
public interface WeatherVisitor  extends numberunit._visitor.NumberUnitVisitor {

  /**
   * Sets the visitor to use for handling and traversing nodes.
   * This method is not implemented by default and visitors intended for reusage
   * in other languages should override this method together with
   * {@link #getRealThis()} to make a visitor composable.
   * RealThis is used to allow visitor composition, where a delegating visitor
   * utilizes this setter to set another visitor as the handle/traversal
   * controller. If this method is not overridden by the language developer,
   * the visitor still can be reused, by implementing this method in a
   * decorator.
   * @param realThis the real instance to use for handling and traversing nodes.
   * @see WeatherDelegatorVisitor 
   */
  default public void setRealThis(WeatherVisitor realThis) {
    throw new UnsupportedOperationException("0xA7011_093 The setter for realThis is not implemented. You might want to implement a wrapper class to allow setting/getting realThis.");
  }

  /**
   * By default this method returns {@code this}. Visitors intended for reusage
   * in other languages should override this method together with
   * {@link #setRealThis(WeatherVisitor)} to make a visitor
   * composable.
   * See {@link #setRealThis(WeatherVisitor)} for more information.
   * @see #setRealThis(WeatherVisitor)
   * @see WeatherDelegatorVisitor
   */
  default public WeatherVisitor getRealThis() {
    return this;
  }
  
  /* ------------------------------------------------------------------------*/
  
  /**
   * By default this method is not called, because the default visitor only
   * visits a node in its dynamic runtime type. Use an InheritanceVisitor
   * if you want to visit a node in its super types as well.
   *
   * @param node the node that is entered 
   */
  default public void visit(ASTNode node) {
  }

  /**
   * By default this method is not called, because the default visitor only
   * visits a node in its dynamic runtime type. Use an InheritanceVisitor
   * if you want to visit a node in its super types as well.
   *
   * @param node the node that is left 
   */
  default public void endVisit(ASTNode node) {
  }
  
  /* ------------------------------------------------------------------------*/
  
  
      
      default public void visit(weather._ast.ASTTemperature node) {}
      
      default public void endVisit(weather._ast.ASTTemperature node) {}
      
      default public void handle(weather._ast.ASTTemperature node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTTemperature node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTHumidity node) {}
      
      default public void endVisit(weather._ast.ASTHumidity node) {}
      
      default public void handle(weather._ast.ASTHumidity node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTHumidity node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTPressure node) {}
      
      default public void endVisit(weather._ast.ASTPressure node) {}
      
      default public void handle(weather._ast.ASTPressure node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTPressure node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTWindstrength node) {}
      
      default public void endVisit(weather._ast.ASTWindstrength node) {}
      
      default public void handle(weather._ast.ASTWindstrength node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTWindstrength node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTWinddirection node) {}
      
      default public void endVisit(weather._ast.ASTWinddirection node) {}
      
      default public void handle(weather._ast.ASTWinddirection node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTWinddirection node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTPrecipitationtype node) {}
      
      default public void endVisit(weather._ast.ASTPrecipitationtype node) {}
      
      default public void handle(weather._ast.ASTPrecipitationtype node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTPrecipitationtype node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTPrecipitationamount node) {}
      
      default public void endVisit(weather._ast.ASTPrecipitationamount node) {}
      
      default public void handle(weather._ast.ASTPrecipitationamount node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTPrecipitationamount node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTClouding node) {}
      
      default public void endVisit(weather._ast.ASTClouding node) {}
      
      default public void handle(weather._ast.ASTClouding node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTClouding node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTSight node) {}
      
      default public void endVisit(weather._ast.ASTSight node) {}
      
      default public void handle(weather._ast.ASTSight node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTSight node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTWeatherPhenomena node) {}
      
      default public void endVisit(weather._ast.ASTWeatherPhenomena node) {}
      
      default public void handle(weather._ast.ASTWeatherPhenomena node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTWeatherPhenomena node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTOpticalPhenomena node) {}
      
      default public void endVisit(weather._ast.ASTOpticalPhenomena node) {}
      
      default public void handle(weather._ast.ASTOpticalPhenomena node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTOpticalPhenomena node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTArtificialPhenomena node) {}
      
      default public void endVisit(weather._ast.ASTArtificialPhenomena node) {}
      
      default public void handle(weather._ast.ASTArtificialPhenomena node) {
        getRealThis().visit(node);
          getRealThis().traverse(node);
        getRealThis().endVisit(node);
      }
    
      default public void traverse(weather._ast.ASTArtificialPhenomena node) {
        // One might think that we could call traverse(subelement) immediately,
        // but this is not true for interface-types where we do not know the
        // concrete type of the element.
        // Instead we double-dispatch the call, to call the correctly typed
        // traverse(...) method with the elements concrete type.

      }
      
      default public void visit(weather._ast.ASTWeatherNode node) {}
      
      default public void endVisit(weather._ast.ASTWeatherNode node) {}
      
      default public void handle(weather._ast.ASTWeatherNode node) {
        getRealThis().visit(node);
        // no traverse() for interfaces and enums, only concrete classes are traversed
        getRealThis().endVisit(node);
      }
    
    
}
