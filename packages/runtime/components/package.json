{
  "name": "@embeddedmontiarc/sol-runtime-components",
  "description": "",
  "version": "2019.6.25-SNAPSHOT",
  "files": ["src", "lib"],
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "build": "tsc -p compile.tsconfig.json",
    "watch": "tsc -p compile.tsconfig.json -w"
  },
  "dependencies": {
    "@theia/core": "latest",
    "@theia/filesystem": "latest",
    "@theia/workspace": "latest",
    "autobind-decorator": "^2.4.0",
    "inversify": "^4.14.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^3.4.5"
  },
  "theiaExtensions": [
    {
      "frontend": "lib/browser/components-frontend-module"
    }
  ]
}
