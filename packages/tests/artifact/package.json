{
  "private": true,
  "name": "@embeddedmontiarc/sol-tests-artifact",
  "version": "2019.6.25-SNAPSHOT",
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "build": "tsc -p compile.tsconfig.json",
    "watch": "tsc -p compile.tsconfig.json -w"
  },
  "dependencies": {
    "@embeddedmontiarc/sol-runtime-artifact": "^2019.6.25-SNAPSHOT",
    "inversify": "^4.14.0"
  },
  "devDependencies": {
  },
  "theiaExtensions": [
    {
      "backend": "lib/node/artifact-backend-module"
    }
  ]
}
