/* (c) https://github.com/MontiCore/monticore */
package testing;

// when having no parent this is non-virtual
virtual direct feedthrough component VirtDF {
    ports
        in Q in1,
        out Q out1;

    connect in1 -> out1;
}