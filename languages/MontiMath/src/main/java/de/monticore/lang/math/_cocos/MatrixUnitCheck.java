/* (c) https://github.com/MontiCore/monticore */
package de.monticore.lang.math._cocos;

//import de.monticore.lang.math._ast.ASTMatrix;


/**
 * checks the units for this symbol
 */
public class MatrixUnitCheck /*implements MathASTMatrixCoCo*/ {
    /*@Override
    public void check(ASTMatrix node) {

        //if the Symbol for the matrix is present we check if the units are consistant
            if(node.getSymbol().isPresent()){
                MathVariableDeclarationSymbol tmp = (MathVariableDeclarationSymbol)node.getSymbol().get();
                if(tmp.getValue().isIncompUnit()){
                    Log.error("0xMATH01 Matrix has entries with different Units");
                }
            }
    }*/
}
