{
  "private": true,
  "name": "@embeddedmontiarc/sol-tests-templates",
  "version": "2020.5.5-SNAPSHOT",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "build": "tsc -p compile.tsconfig.json",
    "watch": "tsc -p compile.tsconfig.json -w"
  },
  "dependencies": {
    "@embeddedmontiarc/sol-runtime-templates": "^2020.5.5-SNAPSHOT",
    "inversify": "^5.0.1"
  },
  "devDependencies": {
  },
  "theiaExtensions": [
    {
      "backend": "lib/node/templates-backend-module"
    }
  ]
}
