package invalid

ide ExcludeNeeded {
    registry "some/registry"
    build "some/build/folder" resolved from CWD

    include module type ExcludeNeededModule

    exclude configuration type ExcludeNeededConfiguration
}
