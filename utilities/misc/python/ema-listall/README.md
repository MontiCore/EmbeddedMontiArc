This scipt lists all projects and project id's of the EMA ecosystem.

To call it please use `start.bat`. The script first installs the required python-gitlab package and then calls the actual script.

The script was tested using Python 3.