package classifier;

component Connector {

  ports in Z(0:255)^{1, 28, 28} image,
        out Q res;

  instance Network<10> predictor;
  instance ArgMax<10> max;

  connect image -> predictor.data;
  connect predictor.softmax -> max.inputVector;
  connect max.maxIndex -> res;
}
