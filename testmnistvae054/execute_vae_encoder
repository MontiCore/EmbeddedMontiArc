    vector<float> data_ = CNNTranslator::translate(data);

    vector<float> encoding_(8);


    _predictor_0_.predict(data_, encoding_);

    encoding = CNNTranslator::translateToCol(encoding_, std::vector<size_t> {8});
