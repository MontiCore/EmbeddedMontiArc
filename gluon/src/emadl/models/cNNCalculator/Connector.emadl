/* (c) https://github.com/MontiCore/monticore */
package cNNCalculator;

component Connector {
    ports in Q(0:1)^{100, 1, 1} noise,
          out Q(0:1)^{1, 28, 28} res;

    instance Generator predictor;
    connect noise -> predictor.data;
    connect predictor.softmax -> res;
}
