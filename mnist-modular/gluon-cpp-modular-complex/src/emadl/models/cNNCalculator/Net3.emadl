package cNNCalculator;

component Net3 {
    ports in Z(0:255)^{500} data,
         out Q(0:1)^{10} dataOutput;

     implementation CNN {
            data ->
            Relu() ->
            Dropout() ->
            FullyConnected(units=10) ->
            dataOutput;
        }
}