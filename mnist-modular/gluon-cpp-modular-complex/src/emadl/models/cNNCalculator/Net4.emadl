package cNNCalculator;

component Net4 {
    ports in Q(0:1)^{10} dataInput,
         out Q(0:1)^{10} softmax;

     implementation CNN {
            dataInput ->
            Softmax() ->
            softmax;
        }
}