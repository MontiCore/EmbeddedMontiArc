package letterpred;

component Net3 {
    ports in Z(0:255)^{500} data,
         out Q(0:1)^{26} dataOutput;

     implementation CNN {
            data ->
            Relu() ->
            Dropout() ->
            FullyConnected(units=26) ->
            dataOutput;
        }
}