/* (c) https://github.com/MontiCore/monticore */
package pumpStationExample;

view NegativeEffectorEdited {
    component SensorReadingEdited {
        ports 
            in ? $inPort,
            out boolean unused;
    }
    
    effect SensorReadingEdited.$inPort -> SensorReadingEdited.unused;
}
