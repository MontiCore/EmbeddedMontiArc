package de.monticore.lang.monticar.cnnarch.generator.decomposition;

public enum LayerType {
    INPUT,
    PARALLEL_INPUT,
    DEFAULT,
    PARALLEL_DEFAULT,
    OUTPUT,
    PARALLEL_OUTPUT,
    FUNCTION
}
