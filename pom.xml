<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>de.monticore.lang.monticar</groupId>
    <artifactId>ema-lsp</artifactId>
    <version>1.4-SNAPSHOT</version>

    <packaging>jar</packaging>

    <properties>
        <se-commons.version>1.7.8</se-commons.version>
        <embedded-montiarc.version>0.1.19-SNAPSHOT</embedded-montiarc.version>
        <embedded-montiarc-math.version>0.1.13-SNAPSHOT</embedded-montiarc-math.version>
        <embedded-montiarc-deeplearning.version>0.2.10-SNAPSHOT</embedded-montiarc-deeplearning.version>
        <cnn-train.version>0.3.8-SNAPSHOT</cnn-train.version>
        <cnn-arch.version>0.3.3-SNAPSHOT</cnn-arch.version>
        <se-commons-utilities.version>1.7.8</se-commons-utilities.version>
        <monticore-lsp-commons.version>1.13-SNAPSHOT</monticore-lsp-commons.version>
        <junit.version>5.5.2</junit.version>
        <commons-io.version>2.6</commons-io.version>

        <!--Misc-->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>de.se_rwth.commons</groupId>
            <artifactId>se-commons-logging</artifactId>
            <version>${se-commons.version}</version>
        </dependency>
        <dependency>
            <groupId>de.monticore.lang.monticar</groupId>
            <artifactId>embedded-montiarc</artifactId>
            <version>${embedded-montiarc.version}</version>
        </dependency>
        <dependency>
            <groupId>de.monticore.lang.monticar</groupId>
            <artifactId>embedded-montiarc-math</artifactId>
            <version>${embedded-montiarc-math.version}</version>
        </dependency>
        <dependency>
            <groupId>de.monticore.lang.monticar</groupId>
            <artifactId>embedded-montiarc-deeplearning</artifactId>
            <version>${embedded-montiarc-deeplearning.version}</version>
        </dependency>
        <dependency>
            <groupId>de.monticore.lang.monticar</groupId>
            <artifactId>cnn-train</artifactId>
            <version>${cnn-train.version}</version>
        </dependency>
        <dependency>
            <groupId>de.monticore.lang.monticar</groupId>
            <artifactId>cnn-arch</artifactId>
            <version>${cnn-arch.version}</version>
        </dependency>
        <dependency>
            <groupId>de.se_rwth.commons</groupId>
            <artifactId>se-commons-utilities</artifactId>
            <version>${se-commons-utilities.version}</version>
        </dependency>
        <dependency>
            <groupId>de.monticore.util</groupId>
            <artifactId>monticore-lsp-commons</artifactId>
            <version>${monticore-lsp-commons.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons-io.version}</version>
        </dependency>
        <!-- test-->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.22.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.0</version>
                <configuration>
                    <source>8</source>
                    <target>8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <id>se-nexus</id>
            <url>https://nexus.se.rwth-aachen.de/content/repositories/embeddedmontiarc-releases/</url>
        </repository>
        <snapshotRepository>
            <id>se-nexus</id>
            <url>https://nexus.se.rwth-aachen.de/content/repositories/embeddedmontiarc-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>
</project>
