package mnistvae;

component Decoder{
    ports in Z(0:255)^{2} data,
          out Q(-oo:oo)^{784} softmax;

    implementation CNN {
        data ->
        FullyConnected(units=400) ->
        FullyConnected(units=784) ->
        Sigmoid() ->
        softmax;
    }
}